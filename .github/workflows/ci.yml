name: CI

on:
  pull_request:
  push:

jobs:
  Build:
    strategy:
      matrix:
        os: &platforms [&artifact_os ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Dependencies
        uses: ./.github/actions/deps
        timeout-minutes: 15

      - name: Build
        run: make build

      - name: Upload release artifact
        env:
          artifact_os: *artifact_os
        if: matrix.os == env.artifact_os
        uses: actions/upload-artifact@v7
        with:
          name: release
          path: build-release/module_*/*.s?prx
          if-no-files-found: error

      - name: Upload debug artifact
        env:
          artifact_os: *artifact_os
        if: matrix.os == env.artifact_os
        uses: actions/upload-artifact@v7
        with:
          name: debug
          path: build-debug/module_*/*.s?prx
          if-no-files-found: error

  Test:
    needs: Build
    strategy:
      matrix:
        os: *platforms
    runs-on: ${{ matrix.os }}
    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Dependencies
        uses: ./.github/actions/deps

      - name: Test
        run: make test

  Lint:
    needs: Build
    strategy:
      matrix:
        os: *platforms
    runs-on: ${{ matrix.os }}
    steps:

      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Dependencies
        uses: ./.github/actions/deps

      - name: Lint
        run: make lint
